# Security Policy

이 저장소는 개인 PC에서 실행하는 로컬 우선 연구 도구입니다. 인터넷에 직접 공개하는 다중 사용자 서비스로 설계되지 않았습니다.

## 기본 원칙

- 기본 바인딩 `127.0.0.1`을 유지하세요.
- LAN 또는 사설망에서 접속할 때는 충분히 긴 임의의 `QUANT_ACCESS_TOKEN`을 설정하세요.
- 공유기 포트포워딩으로 서버를 공인 인터넷에 직접 노출하지 마세요.
- HTTPS 종단, 사용자별 권한, 감사 로그가 필요한 환경에는 별도의 보안 프록시와 운영 설계가 필요합니다.
- `.cache/`, `dist/`, `android-app/private.properties`, `android-app/local.properties`, `android-app/keystore.properties`, `*.jks`, `*.keystore`를 커밋하지 마세요.
- 로그, 스크린샷과 이슈에 토큰이 포함된 URL, 실제 계좌 정보 또는 개인 서버 주소를 붙이지 마세요.

## 노출이 의심될 때

1. 노출된 토큰이나 서명 비밀번호를 즉시 폐기·교체합니다.
2. 해당 값이 들어간 APK는 새 토큰으로 다시 빌드합니다.
3. Git 기록에 비밀값이 들어갔다면 단순 삭제 커밋만으로 충분하지 않으므로 기록 정리와 자격증명 교체를 함께 수행합니다.
4. 재현 자료에서는 비밀값, 로컬 절대경로와 실제 포트폴리오를 제거합니다.

## 취약점 제보

공개 GitHub 이슈에는 비밀값이나 실제 접속 주소를 적지 마세요. 공격 절차보다 영향 범위, 영향을 받는 버전, 비식별화한 최소 재현과 기대 동작을 먼저 남겨 주세요. 즉시 악용 가능한 비밀이 포함되어 있다면 먼저 해당 비밀을 폐기한 뒤 제보하세요.

## 범위 밖

- 편의 데이터 공급자의 값 지연·정정·누락
- 투자 점수 또는 모델 성과에 대한 이견
- 사용자가 직접 공인 인터넷에 노출한 서버의 운영 문제
- 제3자 Android APK나 변경된 빌드에서 발생한 문제

데이터 정확성 문제는 보안 문제와 별도로 재현 가능한 티커, 기준일, 공급자와 기대값을 함께 기록해 주세요.
